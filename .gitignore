__pycache__/
*.pyc
.DS_Store
.claude/

# E0.5 合成影像是 11_e05_prepare_images.py 從固定 seed 決定性產生的，可重新產生，
# 不需要進 git（2500 張 PNG，~250MB）。results/e05_image_manifest.csv、
# e05_embeddings.npz、e05_report.md 才是需要保留的產物。
data/e05_synthetic/

# 全部 60 支影片、6000 張抽樣影格像素資料（~2GB），由 09_pilot_sample_frames.py
# 用固定 SAMPLE_SEED 決定性重新抽樣、下載得到，可重新產生，不需要進 git。
# pilot_sampled_frames.csv / pilot_frame_labels.csv 才是需要保留的分析結果。
data/sampled_frames/

# 09_pilot_sample_frames.py 每支影片下載/抽樣完當下寫的逐支影片 checkpoint
# （方便中斷後 rebuild_from_existing() 復原），內容已經合併進
# results/pilot_sampled_frames.csv，不需要另外進 git。
results/pilot_sampled_frames_*.csv
